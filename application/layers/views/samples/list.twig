{% extends "layout.twig" %}

{% block content %}
    <h1>Samples/list</h1>
    
    <ul>
    {% for item in 1..10 %}
        <li>
            <a href="{{ linkTo('samples_get', {title: 'titlevalue', id: loop.index}) }}">Item {{ loop.index }}</a>
        </li>
    {% endfor %}
    </ul>
{% endblock%}